#nullable enable

namespace Surtr
{
    /// <summary>
    /// Build/version metadata for the Surtr runtime. Placeholder to confirm the
    /// project compiles end to end; expand as the real versioning story lands.
    /// </summary>
    public static class SurtrInfo
    {
        public const string Version = "0.0.1";
    }
}
